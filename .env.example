MONGODB_URI=mongodb://localhost:27017/techverx
PORT=3000

# Required for AI chat, skill inference assist, and semantic search
OPENAI_API_KEY=sk-your-key-here

# Optional overrides
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Authentication (required — generate secret: openssl rand -hex 32)
AUTH_PASSWORD=your-strong-shared-password
AUTH_SECRET=your-random-32-char-or-longer-secret
